apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: sandbox-ui-deployment
  name: sandbox-ui-deployment
spec:
  selector:
    matchLabels:
      app: sandbox-ui
  template:
    metadata:
      labels:
        app: sandbox-ui
    spec:
      containers:
        - image: APP_IMAGE
          name: container
          volumeMounts:
            - name: sandbox-ui-nginx-config-volume
              mountPath: "/etc/nginx/nginx.conf"
              subPath: "nginx.conf"
              readOnly: true
          imagePullPolicy: Always
          envFrom:
            - secretRef:
                name: sandbox-ui-environment
          resources:
            requests:
              cpu: 30m
              memory: 50M
            limits:
              cpu: 30m
              memory: 50M
      volumes:
        - name: sandbox-ui-nginx-config-volume
          configMap:
            name: sandbox-ui-nginx-conf
